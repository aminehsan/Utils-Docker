services:
  ollama:
    container_name: ollama
    image: ollama/ollama:latest # ollama/ollama:latest -> ollama/ollama:0.31.1
    restart: unless-stopped
    tty: true
    ports:
      - "11434:11434"
    environment:
      - TZ=Asia/Tehran
    volumes:
      - ollama_data:/root/.ollama

  open-webui:
    container_name: open-webui
    image: ghcr.io/open-webui/open-webui:main
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Tehran
      - OLLAMA_BASE_URL=http://ollama:11434
    volumes:
      - open_webui_data:/app/backend/data
    depends_on:
      - ollama

volumes:
  ollama_data:
  open_webui_data:
